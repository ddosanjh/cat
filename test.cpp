#include <stdio.h>
#include "command.h"

/*

comment out after to avoid breaking Arduino compile 

int main() {

    int btns[5] = {BTN_ONE, BTN_TWO, BTN_THREE, BTN_FOUR, BTN_FIVE};
    Command::provideTestInput(btns, 5);


    int cmd = Command::dequeue();
    printf("dequeued command: %X(%d)\n", cmd, cmd);

    int btns_2[3] = {BTN_OK, BTN_TWO, BTN_THREE};

    return 0;
}
*/